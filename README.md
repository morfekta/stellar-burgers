# Проектная работа "Stellar Burgers"

<img width="1920" height="1000" alt="image" src="https://github.com/user-attachments/assets/7e62f829-a048-4794-a6ee-7feb45b85b0d" />

## О проекте 

Stellar Burgers — SPA-приложение космической бургерной.  
Пользователь может собрать бургер из ингредиентов, оформить заказ, зарегистрироваться и авторизоваться, восстановить пароль и просматривать свои заказы в личном кабинете.

Этот проект был разработан в рамках курса "Фронтенд-разработчик" в Яндекс Практикуме. Основной фокус — React, TypeScript, управление состоянием через Redux и работа с REST API.

## 🔗 Ссылки

- [Макет в Figma](https://www.figma.com/file/vIywAvqfkOIRWGOkfOnReY/React-Fullstack_-Проектные-задачи-(3-месяца)_external_link?type=design&node-id=0-1&mode=design)

## Реализованный функционал

- Конструктор бургеров:
  - отображение списка ингредиентов с сервера;
  - сборка бургера;
  - подсчёт итоговой стоимости заказа.
- Детали ингредиентов:
  - модальные окна с подробной информацией (КБЖУ);
  - отдельные страницы ингредиентов, доступные по URL.
- Оформление заказа:
  - отправка состава бургера на сервер;
  - отображение номера заказа и состояния.
- Лента заказов:
  - отображение списка всех заказов сервиса (общая лента);
- Авторизация и регистрация:
  - регистрация нового пользователя;
  - вход/выход;
  - восстановление пароля.
- Личный кабинет:
  - просмотр и редактирование профиля (имя, e-mail, пароль);
  - просмотр истории заказов.
- Навигация:
  - роутинг между страницами конструктора, ленты заказов и профиля;
  - защищённые роуты для личного кабинета и истории заказов.
 
## Мой вклад в проект

Репозиторий основан на шаблоне Яндекс Практикума [`yandex-praktikum/stellar-burgers`](https://github.com/yandex-praktikum/stellar-burgers), но бизнес-логика и значительная часть клиентского кода реализованы мной.

В рамках проекта я:

- Подключила и настроила **Redux** для управления состоянием:
  - хранилище ингредиентов и текущей сборки бургера;
  - состояние пользователя (авторизация, данные профиля);
  - состояние текущего и прошлых заказов.
- Реализовала взаимодействие с API:
  - загрузка ингредиентов;
  - создание заказа с передачей состава бургера;
  - эндпоинты регистрации, логина, обновления токена, получения/обновления профиля.
- Настроила клиентскую **авторизацию**:
  - хранение и обновление access/refresh токенов;
  - приватные роуты для защищённых страниц;
  - редиректы в зависимости от статуса авторизации.
- Настроила **роутинг** (React Router):
  - основные страницы приложения;
  - вложенные маршруты (например, модальные окна с деталями заказа по URL).
- Настроила инфраструктуру проекта:
  - сборка на Webpack;
  - линтеры (ESLint, Prettier).
 
## Технологии и подходы

- **React** — функциональные компоненты, хуки.
- **TypeScript** — типизация стейта, экшенов и пропсов.
- **Redux / Redux Toolkit** — слайсы, асинхронные экшены для запросов к API.
- **React Router** — клиентский роутинг и защищённые роуты.
- **REST API** — работа с эндпоинтами бургер-сервиса, обработка ошибок.
- **CSS-modules** — стилизация компонентов.
- **Storybook** — каталог UI-компонентов.
- **Jest / React Testing Library / Cypress** — модульные и e2e-тесты.

## Архитектура

- **Компоненты** разделены на:
  - «умные» контейнеры, связанные с Redux;
  - «глупые» презентационные компоненты.
- **Состояние** хранится в глобальном Redux-store:
  - срезы для ингредиентов, конструктора, пользователя, заказов.
- **API-слой** — отдельный модуль `burger-api` (обёртка над fetch с базовым URL и обработкой ошибок).
- **Маршрутизация** — единая конфигурация роутов, приватные и публичные страницы.

## Тестирование

- **Unit-тесты** (Jest / React Testing Library):
  - редьюсеры и селекторы Redux;
  - ключевые компоненты (конструктор, формы авторизации).
- **E2E-тесты** (Cypress):
  - сценарий сборки и оформления бургера;
  - сценарий регистрации и логина;
  - сценарий перехода в личный кабинет и выхода из аккаунта.

## Структура проекта

- `src/index.tsx` — точка входа, подключение Redux и роутера.
- `src/services/*` — конфигурация Redux-store, редьюсеры и санки/саги.
- `src/utils/burger-api.ts` — работа с API бургерной.
- `src/components/*` — презентационные компоненты конструктора, заказов и UI.
- `src/pages/*` — страницы приложения (конструктор, лента заказов, профиль и т.п.).

## 📦 Установка и запуск

Для запуска проекта вам понадобится [Node.js](https://nodejs.org/) (версии 16 или выше).

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/morfekta/stellar-burgers.git
   cd stellar-burgers
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Настройка окружения:**
   Создайте файл `.env` в корне проекта (или используйте `.env.example` как шаблон) и добавьте API URL:
   ```env
   BURGER_API_URL=https://norma.nomoreparties.space/api
   ```

4. **Запуск в режиме разработки:**
   ```bash
   npm start
   ```
   Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000).

## 📜 Скрипты

- `npm start` — Запуск dev-сервера.
- `npm run build` — Сборка проекта для продакшена.
- `npm run lint` — Проверка кода линтером (ESLint).
- `npm run test` — Запуск тестов.
